# 缓存与计费设计核对记录

日期：2026-09-22。范围：设计稿与算例，**没有实现计费引擎，没有运行真实模型，也没有声称新功能测试通过**。

## 已完成检查

- 阅读当前 planner、tracing、stats 及配置代码，确认原始 usage 已保留，现有主模型 cost 只采用平坦输入/输出价。
- 查阅 DeepSeek 官方价格、缓存指南和 Chat Completions usage 说明。来源链接随设计稿保存。
- PowerShell `ConvertFrom-Json` 成功解析配套 fixtures。
- 用 PowerShell `[decimal]` 按分项公式计算，并对预期结果执行相等断言；不是浮点近似比较。
- `git diff --check` 无空白错误。仅变更文档和 README 导航。

| 算例 | 已核对结果 |
| --- | --- |
| 8000 命中 + 2000 未命中 + 1000 输出，高峰 | 0.01232 CNY |
| 同用量，空闲 | 0.00616 CNY |
| 高峰同用量、输入全未命中基线 | 0.028 CNY |
| 高峰缓存优惠反事实差额 | 0.01568 CNY |
| 空闲缓存优惠反事实差额 | 0.00784 CNY |
| 输入缓存拆分缺失时，高峰可能范围 | 0.0084–0.028 CNY |
| 100 输入全命中 + 9900 输入全未命中 | 加权 Token 命中率 1% |

这些是设计算术检查，不是供应商真实账单验证。缓存字段冲突处理、日期边界、节假日、价格版本、历史补算、UI 和 API 的用例仍属于待实现清单。旧项目的 48 项后端测试不应作为本功能已经实现的证据。
